<div class="well">
	<h2>Finish these events by Sunday</h2>
</div>
<% if session[:currentUserID] == nil then %>
	<div class="well"><h5>You must log in to complete your solves </h5></div>
<%end%>
<%= render "shared/contest_links" %>
<div class="row">
	<div class="col-lg-6">
	<div class="well">
		<h5>Your solves must be completed by <%= @endDate.to_s %></h5>
	</div>
	</div>
</div>